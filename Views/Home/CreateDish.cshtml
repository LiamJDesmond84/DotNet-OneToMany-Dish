@model DotNet_OneToMany_Dish.Models.Dish


<div>
    <form asp-action="CreateDish" asp-controller="Home" method="post">
        <label asp-for="Name">Name</label>
        <input asp-for="Name" type="text"/>

        <label asp-for="Calories">Calories</label>
        <input asp-for="Calories" type="number" />

        <label asp-for="Tastiness">Tastiness</label>
        <input asp-for="Tastiness" type="number" />

        <label asp-for="Description">Description</label>
        <input asp-for="Description" type="text" />

        <label asp-for="Description">Chef</label>

@*         <select asp-for="Chef" class="form-control" asp-items="@(new SelectList(ViewBag.Chefs))">
            <option>-- Select Client --</option>
        </select> *@
        @* @Html.DropDownListFor(x =>Chef, new SelectList(ViewBag.Chefs, "ChefId")) *@
        @Html.DropDownListFor(x => x.Chef, new SelectList(ViewBag.Chefs, "ChefId", "Name"))
@*          <select asp-for="Chef" name="Chef">

            @{
                foreach(var x in ViewBag.Chefs)
                {
                    <option value="1">@x.Name</option>
                }
            }
           
        </select> *@

        <button type="submit">Create Dish</button>
    </form>
</div>